PKG_NAME=bash
PKG_VERSION=
PKG_DEPENDS=
PKG_SOURCE_URL=#
PKG_BUILD_COMMANDS='
latest=$(wget -qO- https://ftp.gnu.org/gnu/bash/ | grep -o "bash-[0-9\.]*\.tar\.gz" | sort -V | tail -n1)
wget "https://ftp.gnu.org/gnu/bash/$latest" -O "$latest"
tar xf "$latest" --strip-components=1

./configure --prefix=/usr
make DESTDIR="$DESTDIR" install
'